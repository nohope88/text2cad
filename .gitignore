# secrets — NEVER commit
.env

# runtime artifacts (products live on panda, not in git)
out/
logs/
*.log
.heartbeat

# python
.venv/
__pycache__/
*.pyc

# local previews
blocks/preview/

# built binaries (rebuild via integrations/importdesign/build.sh)
bin/
tg/
