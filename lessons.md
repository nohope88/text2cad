# text2cad lessons (auto-maintained — repair sessions append here)
# A lesson that repeats must GRADUATE to code: a golden block, a gate linter
# check, or a brief-template constraint. Graduated lessons are marked [GRADUATED].

- Always fuse every piece into ONE solid with a single union chain, and make each piece OVERLAP its neighbors by >= 1mm — tangent/touching faces do NOT fuse and produce multiple bodies (Huggy 2026-08-10: 13 bodies, not watertight).
- Before exporting, verify in the cad tool JSON that the part is a single connected watertight solid (bodies == 1); never export on "no error" alone.
- [GRADUATED → gate bridge_span check] A wide flat internal ceiling (sealed-chamber roof) is an unsupported bridge that aggregate overhang% cannot see — what matters is local SPAN (inscribed-circle diameter of each down-facing patch), not area: a 16mm slot bridges fine, a 50mm chamber roof sags. Gate now fails span > 25mm (eclipse-v2 printability lens 2026-08-11; retro-caught eclipse-v1 at 52.7mm and validated robot at 23mm).
- [GRADUATED → BUILD prompt numeric-contract rule] Every explicit number the brief states must become an assert in params.py validate() — the build used a 6mm fillet where the brief demanded >=10mm twice (eclipse-v2 fidelity lens 2026-08-11).
- [GRADUATED → DRAFT prompt hero rule] A hero render must make a stranger WANT the object in 3 seconds — not be able to name its category. Fix a badly-reading render with the CAMERA (angle that shows silhouette and mechanism at their most striking), never by simplifying, symmetrising or labelling the object; hard to classify is a win, generic is the failure. This line replaces the 2026-08-11 "stranger must decode it in 3 seconds" wording, which DISCOVER read out of this file and applied as an IDEA filter — it scored down every abstract candidate and is how three straight cycles shipped a pen holder, a phone holder and a dock (terminal-cursor-pen-holder discover.md says so in its own self-critique). Geometry lessons in this file are for BUILD and REPAIR only; DISCOVER is now told not to read them.
- [GRADUATED → gate lint + blocks/cadblocks.rounded_box] Never fillet ALL three edge directions of a box (top + bottom + vertical) at any single corner — whether via one blanket `.edges().fillet(r)` or several chained per-selector fillets that together still cover every corner. OCCT must synthesize a spherical vertex-blend there, and in this environment that blend tessellates to a degenerate zero-area sliver triangle disconnected from the main mesh (~8 phantom bodies per fully-filleted box), passing `is_solid`/no-error checks yet failing the watertight/bodies gate — reproduced with a bare `cq.Workplane("XY").box(10,10,10).edges().fillet(1)`, so it's not fixable by adjusting radius, order, or mesh tolerance. Fix: bake vertical-corner rounding into the 2D profile (arcs via a rounded-rect wire, then `.extrude()`) so no sharp vertex remains, and apply any additional top/bottom-only fillets afterward — those two edge loops never share a vertex with each other so no vertex-blend is ever built (Huggy 2026-08-11 repair).
- A sealed cavity (only a pinhole/bore for access, no post-print reach-in) must never have a flat internal ceiling wider than the safe bridge span — bake the cavity's 2D profile with a peaked "tent" top (two <=45deg roof slopes meeting at a center ridge at the same max height as the old flat top) instead of a flat-topped rounded rect; this drops bridge_span_mm to 0 while leaving the exterior shell and every brief-stated dimension untouched (eclipse-v2 printability repair 2026-08-11, fixing the finding gate.py's bridge_span check now catches automatically). Separately: if a fillet on an exterior seam starts failing with OCCT `StdFail_NotDone` after an unrelated cavity-cut/union change elsewhere in the same solid (even at a radius that worked before), apply that fillet to the raw exterior union BEFORE the cut/union chain, not after — the simpler pre-cut topology is far more likely to solve at the same radius.
- When a brief states two dimensions that geometrically trade off (an appended limb's own length/angle vs. a required clear zone on the trunk it attaches to), check feasibility analytically at brief-authoring time, before build: for a limb revolved/rotated off axis at angle `kink` with length `L` and end radius `r`, its worst-case backward reach over the trunk is `L*cos(180-kink) + r*sin(180-kink)` (endpoint values only — no taper-curve shape or fillet trick changes this, it's fixed by the limb's own stated length/angle/tip-radius). eclipse-v2 (2026-08-11): a 55mm/55deg eyepiece branch with a 34mm hood mathematically reaches back ~46mm over the tube no matter how it's built, leaving only 29 of the brief's required 50mm grip span — two repair passes maxed out every legal construction lever (hood OD at spec floor, junction pinned to the validate() floor next to the screen) and could only buy back 4mm, because the true limiting terms are the limb's own brief-mandated length/angle/tip-size, not the construction. A "change geometry construction only, do not redesign" repair session cannot close a gap that traces back to two explicit, mutually-conflicting brief numbers — that must be caught (and one number relaxed, or the limb shortened) when the brief is written, not discovered post-build.
- A lens-reported "gouge/cavity carved into a face" claim must be checked against the actual B-rep before touching geometry — `scripts/review`'s renderer (`cadpy/render_part.py`, matplotlib mplot3d) has no z-buffer; its own source comment admits the per-triangle backface cull ("every sightline exits through a camera-facing face") assumes no self-occlusion, which is false for a blind hole viewed at an angle that looks partway into its mouth: the hole's interior back-wall triangles are legitimately front-facing per the naive normal·view-dir test yet sit spatially behind the exterior face, so with no depth sort they can paint over it, showing a fake gouge exactly at the hole's back-wall footprint (z-range = hole floor..top, y-range = hole-center ± radius). terminal-cursor-pen-holder (2026-08-12): two prior repair passes (both crashed mid-run) chased this as a real defect; ground truth via `Shape.isInside()` point-sampling the front face on a fine grid (1204 pts, 0 missing) plus an isolated single-face re-tessellation (2 clean triangles, no fan) proved the solid has zero missing material — the "confirmed via raw STL triangle analysis" in the prior review was itself the false-positive, because filtering triangles by `normal.y < -0.5` alone (without also requiring them to lie near the true exterior face's y-plane) matches the interior hole back-walls too. Verify with `isInside()` sampling and/or the tool's true orthographic (non-iso) view — which this renderer draws without the ambiguous grazing angle — before spending a repair pass moving geometry to chase a render-only illusion.
- Every geometry-cutting constant must be traced back to the SAME params.py name the fit-check math uses to build its mock, not a locally hardcoded number that merely looks similar — draft-stack-dock's grip-scallop cutter hardcoded `dish_depth = 3.0` right next to a comment citing the brief's "40 W x 10 D" spec while `params.SCALLOP_D = 10.0` sat unused three lines away in the same function (only reused, mismatched, as a Z-height clip), so the pocket was cut 3mm deep against an 10mm-deep interface mock and clashed 706mm3 vs a 200mm3 budget. Fix was `dish_depth = p.SCALLOP_D + 0.3` (small margin so the mock's own boundary isn't a coincident face); grep every hardcoded numeric literal inside a cutter/feature function against the params.py names the brief/fit_checks derive from it, and if a param exists for that exact quantity, wire it in directly instead of re-deriving or eyeballing a "safe-looking" constant.
- A periodic perpendicular cross-tie grid added only to defeat the gate's flat-quad mesh-tessellation false positive (a long thin down-facing gap between parallel ribs meshes as 2 giant sliver triangles whose centroid reads as far-from-boundary, tripping bridge_span even when the true gap is safe) has a narrow, non-negotiable safe-density window, not a dial you can loosen for cosmetics: draft-stack-dock's z=0-6 "7 front-to-back flow ribs" (brief: uni-directional, no cross members) needed cross-ties every ~17mm pitch to hold bridge_span at 19.2mm — 22mm pitch already only barely passed (24.5mm), and 30mm pitch failed outright (25.6mm). That safe density inescapably reads as a two-directional waffle grid to a fidelity reviewer even though it's structurally sound; restructuring the connectors to be scoped per-gap instead of full-width bars (tried in this repair) changes NOTHING visually or volumetrically, since both approaches only add material inside the open gaps — the full-width portion crossing already-solid rib material was always a no-op union. A repair pass (construction-only, no redesign) cannot buy back this mismatch by thinning tie density; it will just re-fail the gate. This has to be solved at BUILD time with a genuinely different construction for any brief-mandated uni-directional rib/slat feature — e.g. a shallow non-planar (grooved) underside profile running the SAME direction as the ribs, so OCCT's mesher is forced to add interior vertices without introducing a perpendicular structure — instead of defaulting to perpendicular cross-ties as the fix for this false-positive class.

- Always re-derive a fit-check mock's pose AND size from the same params.py names an arbitration amendment touched, and always re-export before trusting a gate verdict — a repair session's last act must be `cad` then `gate.py`, in that order. draft-stack-dock (2026-08-12) failed the FUNCTIONAL tier on two artifacts that were both stale rather than wrong: (a) `bridge_span=45.7` had already been fixed in main.py by the previous session's rail backing webs, but that session ended without re-exporting, so the gate re-measured a 4-minute-old STL and burned a whole repair tier re-diagnosing a solved problem (re-export alone: 45.7 -> 18.3); (b) `fit_checks.py`'s finger-pad mock still hardcoded an 8mm-deep pad starting 2mm inside the face — geometry derived from the PRE-arbitration `SCALLOP_D = 10`, left untouched when rev3 amendment 3 shallowed the scallop to 40x4 over a 6.0mm local wall. That made the check demand 10mm of clearance inside a 6.0mm wall, i.e. it demanded the exact skirt breach must-have #8 forbids — unsatisfiable by ANY geometry, so no "construction-only" repair could ever pass it. Rebuilding the mock as `box(p.SCALLOP_D, pad_w, pad_h)` seated at the outer face kept every defect the check existed to catch (measured on the real mesh: 3mm dish -> 355mm3 FAIL, 2mm -> 595 FAIL, none -> 960 FAIL, as-built 4.3mm -> 56 OK) plus a new witness-block assert that the wall behind the dish is still solid. When an amendment changes a number, grep BOTH directions — every cutter that consumes it and every mock that asserts against it — because a mock frozen at the old value fails closed and looks exactly like a geometry bug.

- [GRADUATED → gate.py stale_stl check] The "re-export before trusting a gate verdict" lesson above was already written down and got ignored again the very next session on the SAME part: draft-stack-dock's next repair inherited a `gate.json` (`not_watertight`, `fit_check_failed`) that was stale by 12 minutes against `main.py`'s last edit — `stat` on `main.py`/`draft-stack-dock.stl`/`gate.json` showed `main.py` saved AFTER the STL/step export, and gate ran right after that without a re-export in between. A bisection that rebuilt `gen_step()` stage-by-stage and freshly re-exported found the CURRENT code was already watertight (0 non-manifold edges via a trimesh edge-count-!=2 check) with a passing fit-check; the on-disk STL was just old. Root cause of why re-export got skipped: this part's `gen_step()` takes ~65s (many chained unions/cuts), but `scripts/cad`'s default `--wall-clock-s` is 30 — a plain `cad .` call SANDBOX_TIMEOUTs, and it's easy to read that as "can't re-export right now" and move on to editing/diagnosing instead of retrying with a longer timeout, leaving the stale STL in place for the gate to score. Two takeaways: (1) treat a lesson that repeats as a signal to graduate it — a session-start check comparing `mtime(main.py) > mtime(<slug>.stl)` (or gate.py refusing to score a stale STL) would have caught this mechanically instead of depending on the agent remembering prose guidance twice; (2) if `scripts/cad` SANDBOX_TIMEOUTs on a part with many chained boolean ops, retry with `--wall-clock-s <bigger>` (e.g. 180) before assuming the build is broken — a timeout is not a build failure, and moving on without a successful export is how a gate verdict goes stale.
- The slice-gate printer (Bambu A1 mini profile) hard-rejects footprints wider than 160mm even though the bed is nominally 180x180 (excluded corner region; OrcaSlicer -102 with the real reason hidden in result.json — gate now surfaces it). Every brief envelope must keep TOTAL bbox incl. lugs/skirt/chamfers <= 160x160; verified empirically with test boxes 2026-08-12 (160 passes, 164/170 fail), cost draft-stack-dock one full build attempt.
- Never let an independently-built rib/tie boundary be trimmed against a surface that ANOTHER cutter has already carved edges into — give it a standoff instead. draft-stack-dock (2026-08-13) failed `not_watertight` on 16 non-manifold triangles because `build_plenum_ribs` runs fixed-X ribs straight across the flue, so a rib's flat side face at |x| = DECK_RIB_PITCH + DECK_RIB_T/2 = 25.2 was trimmed against the flue's outer cone — and that trim curve's angular position sweeps 36.5° (z=8.4) → 30.4° (z=16.1), passing within **0.011°** of the lattice's own column-1 window edge at 35.653°. Two independently-built CUT BOUNDARIES that near each other make OCCT place a facet vertex 0.005mm from the trim edge and emit zero-area sliver triangles (measured: 0.001mm² faces at (25.200,18.077,9.568) vs (25.205,18.080,9.568)) — `isSolid` stays true, the STEP looks fine, and only the STL is broken. Rotating the pattern only relocates the coincidence, because the crossing curve sweeps through every window-edge angle somewhere in z; the categorical fix is a `flue_clearance_cutter`-style offset-surface standoff subtracted from every rib/tie piece before union (same technique as the existing `corner_clearance_cutter`), so a rib boundary can never reach the cut-up surface at all. Crucially, do NOT then chase every remaining tangency: a boundary merely GRAZING one smooth revolved surface is benign (verified — 2mm standoff leaves the 25.2 rib face tangent at z≈38 and a cross-tie face tangent at z≈35.6, and the mesh still measures 0 broken faces / 0 non-manifold edges), and the only gaps that remove all tangency here were 9.4–14.8mm, big enough to reopen the deck's bridge span. The hazard is boundary-on-boundary, not boundary-on-surface. Corollary caught the same round: a plug/boss meant to swallow another solid's edge must STRICTLY contain it — the flue-mouth floor plug was `FLUE_MOUTH_D/2 + 2.0` == 32.0 == exactly `flue_radius(FLUE_Z0, True)`, i.e. the flue's bottom edge circle sat precisely on the plug's rim; use `+ WALL_FLUE + 1.0` so it is buried 1mm inside.

- A `lens:<name> FAIL no output` entry in a repair session's failure list means that reviewer AGENT crashed (`is_error: true`, zero verdict text) — it is not a content finding and carries no diagnosis to act on. Treat it as orthogonal to `gate.json`: `gate.py` has no notion of lens results at all (mesh/slice/fit-check only), so a crashed lens cannot be "fixed" by changing geometry. draft-stack-dock (2026-08-13, BROKEN-tier repair attempt 2/2): inherited failure list `['stale_stl', 'bodies=3', 'lens:fidelity FAIL no output']`, but a prior round had already fixed `bodies=3` (via the boundary-on-boundary standoff technique above) and re-exported — confirmed by an independent fresh re-export reproducing an IDENTICAL volume to the pre-existing `gate.json`, plus both the printability and sellability lenses independently re-measuring the same fresh mesh (0 broken faces, bodies=1) and passing. Only fidelity had crashed, with no geometry cause found or needed. Lesson: when a `lens:X FAIL no output` shows up alongside otherwise-clean `gate.json` numbers and passing sibling lenses, don't invent a redesign to satisfy unstated "fidelity concerns" — verify the mesh gate is genuinely fresh and passing (re-export + re-gate, per the stale-STL rule above) and stop; the crashed lens needs a retry, not a fix.
- [GRADUATED → text2cad run_panel() auto-retry, commit 9ebbdbe 2026-08-14] `lens:fidelity FAIL no output` had recurred a THIRD time on draft-stack-dock across separate repair sessions, each time with `gate.json` fresh/passing (bodies=1, watertight, fit_checks pass) and both sibling lenses (printability, sellability) independently passing on the same mesh — i.e. the crash is not correlated with any geometry state, ruling out a part-specific cause. A repair session re-exporting and re-gating (as the stale-STL lesson above prescribes) correctly confirmed "nothing to fix" every time, but that still burned a full repair-tier attempt per occurrence. Fixed in the orchestrator: `run_panel()` now retries a lens call once when `lens_verdict()` returns `"FAIL no output"` before writing it into the panel verdict/failure list — a transient agent crash no longer consumes a repair-attempt budget.
- When adding hidden internal support ribs/gussets to fix a bridge-span finding, first map out every OTHER part that shares the same local volume in the assembled pose — a construction-only fix that ignores this can silently trade one gate failure for a worse one (or a new collision) the gate doesn't even check by default. arc-coil-blaster-prop (2026-08-13): the printability lens correctly found index_wheel's flat "cup form" web bridging 28.8mm unsupported (gate's own inscribed-circle heuristic undercounted it as 16.5mm because 6 isolated lightening holes fragment the visible area without touching either support boundary). The obvious fix — hidden radial ribs from the axle hub out to the rim, filling the hollow rim tube the web caps — is correct in spirit but a naive full-radius version collided with the CARRIER's ratchet hub, which (via the assembled pose's Y-offset math) pokes a full-360° r=23mm cylinder into that exact hollow for that part's entire rim height; the `cad` tool's own assembly-interference check caught it (6330mm^3 overlap) even though gate.py (mesh-only, no cross-part checks) would have passed the STL standalone. A second hard constraint stacked on top: the wheel's drive_pawl is a COMPLIANT FLEXURE (not a rigid mount) over a specific angular arc — fusing rib material there would rigidify it and break the ratchet-and-pawl indexing mechanism, a functional must-have no mesh gate can detect either. Fix was a single C-shaped washer gusset, inner radius = ratchet hub radius + 2mm clearance, outer radius = 1mm into the rim's solid band, angular extent = full circle minus the flexure arm's sweep (with ~10deg margin) — verified with BOTH gate.py (bridge_span 28.8->16.5) AND a full assembly re-export through the `cad` tool (no collision warning) before trusting the fix. Lesson: a bridge/void fix that adds material to a "hollow" region must be checked against (a) every OTHER part whose assembled envelope reaches into that same local space, via the `cad` tool's interference check, not just gate.py's single-part mesh stats, and (b) any COMPLIANT/flexure feature whose motion depends on that region staying unfused.
- When a likeness or fidelity finding says the concept/brief shows a feature the model lacks ENTIRELY (e.g. a pistol grip with trigger guard), ADD that geometry as new construction in the part that owns it — "do not redesign" forbids replacing the approved design, it never excuses omitting a briefed feature; leaving it missing is the failure.
- Before treating a likeness/fidelity "feature X is missing" finding as an ADD-geometry task, check whether brief.md forbids X or whether X's absence is the resolved side of a brief conflict already asserted in params.py — an explicit feature BAN, or a number the layout could not honour, is exactly as unbuildable as two conflicting dimensions and belongs in arbitration, not in a repair round. arc-coil-blaster-prop (2026-08-14, FUNCTIONAL 2/3) inherited `lens:likeness FAIL 2.5/10 "missing the pistol grip/trigger guard entirely"` and the previous session's own lesson saying to just add it; both halves were wrong on the geometry. (a) The grip is NOT missing — measured on the fresh mesh it is the brief's 42x32 oval at 18deg rake dropping 41.5mm below the receiver floor; it merely fails to READ because `validate_layout()` conflict (5) already capped it at 59.5mm = 68% of the brief's 88mm web-to-pinky: the sight rail crest (z=+68.25) and the grip toe (z=-89.5) share ONE 158mm print axis on spine_rear, as-built span 157.75mm with 0.25mm of headroom, and the brief's 88mm column would need 186.25mm, i.e. +28.25mm over the build volume. No construction change can buy that back. (b) The trigger guard is banned twice in brief.md ("no trigger, guard, hammer or safety", and the originality argument vs. a real revolver prop rests on it) AND is mechanically blocked by the gate itself: fit_checks.py `interfaces()` requires 38,766mm3 of EMPTY air (x[-38.1,-12.1], y+/-21, z[-87.5,-52]) directly in front of the grip for the 95th-pct hand, which is precisely the volume a guard bow occupies — so shipping one means editing the brief-derived check that forbids it, i.e. faking the pass. Verify BOTH directions (does the brief mandate it? does a fit-check reserve its space?) before spending a repair tier; and never conclude "feature absent" from a low-res `_assembled.png` — rasterise the real XZ silhouette from the per-part STLs (trimesh triangles -> matplotlib PolyCollection) and look, because a 3/4 render at 900px hides a 41mm protrusion behind a side panel.
